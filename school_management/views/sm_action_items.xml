<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="action_sm_school_info" model="ir.actions.act_window">
            <field name="name">School Information</field>
            <field name="res_model">school.info</field>
            <field name="view_mode">list,form</field>
            <field name="view_id" ref="view_sm_school_info_list"/>
            <field name="help" type="html">
              <p class="o_view_nocontent_smiling_face">
                Add a New School
              </p><p>
                You have to create a school for the very first time only!!
              </p>
            </field>
            <field name="context">{}</field>
        </record>

        <record id="action_sm_class_info" model="ir.actions.act_window">
            <field name="name">class.information</field>
            <field name="res_model">class.info</field>
            <field name="view_mode">list,form</field>
            <field name="view_id" ref="view_sm_class_info_list"/>
            <field name="help" type="html">
              <p class="o_view_nocontent_smiling_face">
                Add a New Class
              </p>
            </field>
            <field name="context">{}</field>
        </record>

    </data>
</odoo>
